import './Start.css'
import Header from "../Header/Header";

const Start = () => {
    return (
        <section className="custom-section start-section">
            <Header/>
        </section>
    );
};

export default Start;
